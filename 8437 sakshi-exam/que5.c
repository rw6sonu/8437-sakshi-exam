#include <stdio.h>
int main()
{
    int i, num, sum = 0, first, last;

    printf("enter the num:- ");
    scanf("%d", num);

    i = 0;
    while (i < num)
    {
        sum = first + last;
        
        sum++;
    }
     printf("sum of first and last digit of num:-%d", sum);
}
